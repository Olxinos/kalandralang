(executable
 (name main)
 (public_name kalandralang)
 (libraries
  uri
  tls
  cohttp-lwt-unix
  lwt
  menhirLib
  ezjsonm
  clap
  re))

(ocamllex lexer)

(menhir
  (modules parser)
  (flags -v --table))
